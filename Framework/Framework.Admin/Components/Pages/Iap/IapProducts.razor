@page "/iap-products"
@attribute [Microsoft.AspNetCore.Authorization.Authorize]
@inherits SafeComponentBase
@using Framework.Admin.Constants
@using Framework.Domain.Enums

<PageTitle>인앱결제 상품 관리</PageTitle>

<h2 class="mb-2">인앱결제 상품 관리</h2>

<div class="alert alert-info py-2 small mb-3">
    <p class="mb-1">스토어별 인앱결제 상품(SKU)을 등록하고 보상 테이블과 연결합니다.</p>
    <p class="mb-0"><strong>[IsEnabled]</strong> false이면 해당 상품의 구매 요청을 처리하지 않습니다.</p>
</div>

<!-- 검색 필터 카드 -->
<section class="card mb-3">
    <div class="card-header py-2"><strong>검색 필터</strong></div>
    <div class="card-body">
        <div class="row g-3">
            <div class="col-md-3">
                <label class="form-label small mb-1">스토어</label>
                <select class="form-select form-select-sm" @bind="filterStore">
                    <option value="">전체</option>
                    @foreach (var s in StoreOptions)
                    {
                        <option value="@s.Value">@s.Label</option>
                    }
                </select>
            </div>
            <div class="col-md-3">
                <label class="form-label small mb-1">상품 유형</label>
                <select class="form-select form-select-sm" @bind="filterProductType">
                    <option value="">전체</option>
                    @foreach (var t in ProductTypeOptions)
                    {
                        <option value="@t.Value">@t.Label</option>
                    }
                </select>
            </div>
            <div class="col-md-3">
                <label class="form-label small mb-1">활성 여부</label>
                <select class="form-select form-select-sm" @bind="filterIsEnabled">
                    <option value="">전체</option>
                    <option value="true">활성</option>
                    <option value="false">비활성</option>
                </select>
            </div>
            <div class="col-md-2">
                <label class="form-label small mb-1">페이지 크기</label>
                <input type="number" class="form-control form-control-sm" @bind="pageSize" min="10" max="100" />
            </div>
        </div>
    </div>
    <div class="card-footer py-2 d-flex gap-2">
        <button class="btn btn-primary btn-sm" @onclick="Search">조회</button>
        <button class="btn btn-outline-secondary btn-sm" @onclick="Reset">초기화</button>
        <button class="btn btn-success btn-sm ms-auto" @onclick="OpenCreateModal">+ 상품 추가</button>
    </div>
</section>

@if (!string.IsNullOrEmpty(errorMessage))
{
    <div class="alert alert-danger py-2 small">@errorMessage</div>
}
@if (!string.IsNullOrEmpty(successMessage))
{
    <div class="alert alert-success py-2 small">@successMessage</div>
}

@if (isLoading)
{
    <p class="text-muted">불러오는 중...</p>
}
else if (result == null)
{
    <p class="text-muted">상단 필터를 설정한 후 <strong>조회</strong> 버튼을 눌러주세요.</p>
}
else if (!result.Items.Any())
{
    <p class="text-muted">조건에 맞는 상품이 없습니다.</p>
}
else
{
    <div class="d-flex align-items-center mb-2">
        <span class="small text-muted ms-auto">총 <strong>@result.TotalCount</strong> 건</span>
    </div>

    <table class="table table-sm table-striped table-hover">
        <thead>
            <tr>
                <th>ID</th>
                <th>스토어</th>
                <th>ProductId (SKU)</th>
                <th>유형</th>
                <th>RewardTableId</th>
                <th>활성</th>
                <th>설명</th>
                <th>관리</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var p in result.Items)
            {
                <tr>
                    <td>@p.Id</td>
                    <td><span class="badge bg-primary">@p.StoreLabel</span></td>
                    <td><code>@p.ProductId</code></td>
                    <td><span class="badge bg-secondary">@p.ProductTypeLabel</span></td>
                    <td>@(p.RewardTableId?.ToString() ?? "없음")</td>
                    <td>
                        @if (p.IsEnabled)
                        {
                            <span class="badge bg-success">활성</span>
                        }
                        else
                        {
                            <span class="badge bg-warning text-dark">비활성</span>
                        }
                    </td>
                    <td>@p.Description</td>
                    <td>
                        <button class="btn btn-outline-primary btn-sm" @onclick="() => OpenEditModal(p)">편집</button>
                        <button class="btn btn-outline-danger btn-sm" @onclick="() => OpenDeleteModal(p)">삭제</button>
                    </td>
                </tr>
            }
        </tbody>
    </table>

    <!-- 페이지네이션 -->
    <div class="d-flex gap-2 align-items-center">
        <button class="btn btn-outline-secondary btn-sm" @onclick="PrevPage" disabled="@(page <= 1)">이전</button>
        <span class="small">@(page) / @(result.TotalPages) 페이지</span>
        <button class="btn btn-outline-secondary btn-sm" @onclick="NextPage" disabled="@(page >= result.TotalPages)">다음</button>
    </div>
}

<!-- 생성 모달 -->
@if (showCreateModal)
{
    <div class="modal show d-block" tabindex="-1" style="background:rgba(0,0,0,0.4)">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title">인앱결제 상품 추가</h5>
                    <button type="button" class="btn-close" @onclick="CloseCreateModal"></button>
                </div>
                <div class="modal-body">
                    <div class="mb-2">
                        <label class="form-label small">스토어 <span class="text-danger">*</span></label>
                        <select class="form-select form-select-sm" @bind="newStore">
                            <option value="">선택...</option>
                            @foreach (var s in StoreOptions)
                            {
                                <option value="@s.Value">@s.Label</option>
                            }
                        </select>
                    </div>
                    <div class="mb-2">
                        <label class="form-label small">ProductId (SKU) <span class="text-danger">*</span></label>
                        <input type="text" class="form-control form-control-sm" @bind="newProductId"
                               placeholder="예: com.example.coins100" />
                    </div>
                    <div class="mb-2">
                        <label class="form-label small">상품 유형 <span class="text-danger">*</span></label>
                        <select class="form-select form-select-sm" @bind="newProductType">
                            @foreach (var t in ProductTypeOptions)
                            {
                                <option value="@t.Value">@t.Label</option>
                            }
                        </select>
                    </div>
                    <div class="mb-2">
                        <label class="form-label small">RewardTableId</label>
                        <input type="number" class="form-control form-control-sm" @bind="newRewardTableId"
                               placeholder="비워두면 보상 없음" />
                    </div>
                    <div class="mb-2">
                        <label class="form-label small">설명</label>
                        <input type="text" class="form-control form-control-sm" @bind="newDescription"
                               placeholder="상품 설명 (Admin 표시용)" />
                    </div>
                    <div class="mb-2 form-check">
                        <input type="checkbox" class="form-check-input" id="newIsEnabled" @bind="newIsEnabled" />
                        <label class="form-check-label small" for="newIsEnabled">활성화</label>
                    </div>
                    @if (!string.IsNullOrEmpty(createError))
                    {
                        <div class="alert alert-danger py-1 small">@createError</div>
                    }
                </div>
                <div class="modal-footer">
                    <button class="btn btn-secondary btn-sm" @onclick="CloseCreateModal">취소</button>
                    <button class="btn btn-success btn-sm" @onclick="Create">생성</button>
                </div>
            </div>
        </div>
    </div>
}

<!-- 편집 모달 -->
@if (showEditModal && editingProduct != null)
{
    <div class="modal show d-block" tabindex="-1" style="background:rgba(0,0,0,0.4)">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title">상품 편집 — @editingProduct.StoreLabel / @editingProduct.ProductId</h5>
                    <button type="button" class="btn-close" @onclick="CloseEditModal"></button>
                </div>
                <div class="modal-body">
                    <div class="mb-2">
                        <label class="form-label small">상품 유형</label>
                        <select class="form-select form-select-sm" @bind="editProductType">
                            @foreach (var t in ProductTypeOptions)
                            {
                                <option value="@t.Value">@t.Label</option>
                            }
                        </select>
                    </div>
                    <div class="mb-2">
                        <label class="form-label small">RewardTableId <span class="text-muted">(제거 시 0 입력)</span></label>
                        <input type="number" class="form-control form-control-sm" @bind="editRewardTableId"
                               placeholder="비워두면 변경 없음" />
                    </div>
                    <div class="mb-2">
                        <label class="form-label small">설명</label>
                        <input type="text" class="form-control form-control-sm" @bind="editDescription" />
                    </div>
                    <div class="mb-2 form-check">
                        <input type="checkbox" class="form-check-input" id="editIsEnabled" @bind="editIsEnabled" />
                        <label class="form-check-label small" for="editIsEnabled">활성화</label>
                    </div>
                    @if (!string.IsNullOrEmpty(editError))
                    {
                        <div class="alert alert-danger py-1 small">@editError</div>
                    }
                </div>
                <div class="modal-footer">
                    <button class="btn btn-secondary btn-sm" @onclick="CloseEditModal">취소</button>
                    <button class="btn btn-primary btn-sm" @onclick="SaveEdit">저장</button>
                </div>
            </div>
        </div>
    </div>
}

<!-- 삭제 확인 모달 -->
@if (showDeleteModal)
{
    <div class="modal show d-block" tabindex="-1" style="background:rgba(0,0,0,0.4)">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title">상품 삭제 확인</h5>
                </div>
                <div class="modal-body">
                    <p><strong>@deletingInfo</strong> 상품을 삭제하시겠습니까?</p>
                    <p class="text-muted small">소프트 삭제로 처리됩니다. (DB 행은 유지)</p>
                </div>
                <div class="modal-footer">
                    <button class="btn btn-secondary btn-sm" @onclick="CancelDelete">취소</button>
                    <button class="btn btn-danger btn-sm" @onclick="ConfirmDelete">삭제</button>
                </div>
            </div>
        </div>
    </div>
}
